import React from "react";
import Main from "./build/app/Main";

/**
 * It is just a wrapper for a Typescript application which resides in ./build folder after compilation
 */
const App = () => <Main />;
export default App;
