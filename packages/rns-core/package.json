{
  "name": "rns-core",
  "version": "0.0.2",
  "private": true,
  "description": "React native shop application core",
  "scripts": {
    "lint": "eslint -c ../../.eslintrc.js --ext .ts,.tsx --ignore-pattern *.d.ts ./src",
    "lint:fix": "eslint -c ../../.eslintrc.js --ext .ts,.tsx --ignore-pattern *.d.ts --fix ./src",
    "format": "prettier --config ../../.prettierrc.js --ignore-path ../../.prettierignore --write src/**/*.ts"
  },
  "dependencies": {
    "@apollo/client": "^3.2.2",
    "components": "0.0.1",
    "immutable": "^4.0.0-rc.12",
    "redux-devtools-extension": "^2.13.8",
    "redux-saga": "^1.1.3",
    "reselect": "^4.0.0",
    "rns-packages": "0.0.1",
    "rns-theme": "0.0.1"
  },
  "devDependencies": {
  }
}
